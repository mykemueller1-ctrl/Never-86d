[1mdiff --git a/client/src/pages/CTapHub.tsx b/client/src/pages/CTapHub.tsx[m
[1mindex 6720dc5..7fc92ef 100644[m
[1m--- a/client/src/pages/CTapHub.tsx[m
[1m+++ b/client/src/pages/CTapHub.tsx[m
[36m@@ -71,6 +71,14 @@[m [mconst DEPT_CONFIG: Record<Department, { label: string; desc: string; icon: any }[m
 [m
 const MANAGER_ROLES = ["owner", "key_manager", "kitchen_manager", "bar_manager"];[m
 [m
[32m+[m[32m// TODO(Facebook Login): Keep Facebook login hidden in production until the frontend loads[m
[32m+[m[32m// the Facebook SDK, calls FB.init with the app ID, and verifies the callback flow end-to-end.[m
[32m+[m[32mconst ENABLE_FACEBOOK_LOGIN = !import.meta.env.PROD && import.meta.env.VITE_ENABLE_FACEBOOK_LOGIN === "true";[m
[32m+[m
[32m+[m[32m// TODO(WebAuthn): Keep biometric/Face ID login hidden until proper credential enrollment,[m
[32m+[m[32m// assertion verification, server-side challenge handling, and recovery flows are implemented.[m
[32m+[m[32mconst ENABLE_BIOMETRIC_LOGIN = false;[m
[32m+[m
 function staffDisplayName(s: SafeStaff): string {[m
   return s.lastName ? `${s.firstName} ${s.lastName}` : s.firstName;[m
 }[m
[36m@@ -112,6 +120,7 @@[m [mexport default function CTapHub() {[m
     } catch { return null; }[m
   });[m
   const [pin, setPin] = useState("");[m
[32m+[m[32m  const [loginError, setLoginError] = useState<string | null>(null);[m
   const [showPin, setShowPin] = useState(false);[m
   const [selectedDept, setSelectedDept] = useState<Department | null>(null);[m
   const [checklistProgress, setChecklistProgress] = useState<Record<string, boolean>>({});[m
[36m@@ -292,16 +301,34 @@[m [mexport default function CTapHub() {[m
     try {[m
       const result = await loginByPin.mutateAsync({ pin: fullPin });[m
       if (result.success && result.staff) {[m
[32m+[m[32m        setLoginError(null);[m
         setStaffUser(result.staff as SafeStaff);[m
         setScreen("welcome");[m
       } else {[m
[32m+[m[32m        const message = result.message || (result.locked ? "Account locked" : "Invalid PIN");[m
         setPin("");[m
[31m-        if (result.locked) toast.error(result.message || "Account locked");[m
[31m-        else toast.error("Invalid PIN");[m
[32m+[m[32m        setLoginError(message);[m
[32m+[m[32m        toast.error(message);[m
       }[m
     } catch {[m
[32m+[m[32m      const message = "Login failed — check connection";[m
       setPin("");[m
[31m-      toast.error("Login failed — check connection");[m
[32m+[m[32m      setLoginError(message);[m
[32m+[m[32m      toast.error(message);[m
[32m+[m[32m    }[m
[32m+[m[32m  };[m
[32m+[m
[32m+[m[32m  const handlePinPadPress = (value: number | string | null) => {[m
[32m+[m[32m    if (value === "⌫") {[m
[32m+[m[32m      setPin((current) => current.slice(0, -1));[m
[32m+[m[32m      return;[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    if (value !== null && pin.length < 4) {[m
[32m+[m[32m      const newPin = pin + value;[m
[32m+[m[32m      setLoginError(null);[m
[32m+[m[32m      setPin(newPin);[m
[32m+[m[32m      if (newPin.length === 4) handlePinLogin(newPin);[m
     }[m
   };[m
 [m
[36m@@ -422,19 +449,19 @@[m [mexport default function CTapHub() {[m
 [m
           {/* Login Mode Tabs */}[m
           <div className="flex gap-1 mb-8 p-1 rounded-xl bg-zinc-900/80">[m
[31m-            <button onClick={() => setLoginMode("pin")}[m
[32m+[m[32m            <button onClick={() => { setLoginMode("pin"); setLoginError(null); }}[m
               className={`flex-1 py-2.5 rounded-lg type-caption font-semibold transition-all ${[m
                 loginMode === "pin" ? "bg-amber-500 text-black" : "text-zinc-500 hover:text-zinc-300"[m
               }`}>[m
               <KeyRound size={14} className="inline mr-1.5 -mt-0.5" />PIN[m
             </button>[m
[31m-            <button onClick={() => setLoginMode("email")}[m
[32m+[m[32m            <button onClick={() => { setLoginMode("email"); setLoginError(null); }}[m
               className={`flex-1 py-2.5 rounded-lg type-caption font-semibold transition-all ${[m
                 loginMode === "email" ? "bg-amber-500 text-black" : "text-zinc-500 hover:text-zinc-300"[m
               }`}>[m
               <Mail size={14} className="inline mr-1.5 -mt-0.5" />Email[m
             </button>[m
[31m-            <button onClick={() => setLoginMode("register")}[m
[32m+[m[32m            <button onClick={() => { setLoginMode("register"); setLoginError(null); }}[m
               className={`flex-1 py-2.5 rounded-lg type-caption font-semibold transition-all ${[m
                 loginMode === "register" ? "bg-amber-500 text-black" : "text-zinc-500 hover:text-zinc-300"[m
               }`}>[m
[36m@@ -452,7 +479,7 @@[m [mexport default function CTapHub() {[m
                     const cfg = DEPT_CONFIG[dept];[m
                     const Icon = cfg.icon;[m
                     return ([m
[31m-                      <button key={dept} onClick={() => setSelectedDept(dept)}[m
[32m+[m[32m                      <button key={dept} onClick={() => { setSelectedDept(dept); setLoginError(null); }}[m
                         className="w-full flex items-center gap-4 p-4 rounded-xl surface-interactive group">[m
                         <div className="w-10 h-10 rounded-xl bg-amber-500/10 flex items-center justify-center group-hover:bg-amber-500/15 transition-colors">[m
                           <Icon size={18} className="text-amber-500" />[m
[36m@@ -484,20 +511,18 @@[m [mexport default function CTapHub() {[m
                     </div>[m
                     <div className="grid grid-cols-3 gap-2">[m
                       {[1,2,3,4,5,6,7,8,9,null,0,"\u232b"].map((n, i) => ([m
[31m-                        <button key={i} onClick={() => {[m
[31m-                          if (n === "\u232b") setPin(p => p.slice(0, -1));[m
[31m-                          else if (n !== null && pin.length < 4) {[m
[31m-                            const newPin = pin + n;[m
[31m-                            setPin(newPin);[m
[31m-                            if (newPin.length === 4) handlePinLogin(newPin);[m
[31m-                          }[m
[31m-                        }} className={`h-12 rounded-xl font-semibold text-base transition-all duration-150 ${[m
[32m+[m[32m                        <button key={i} onClick={() => handlePinPadPress(n)} className={`h-12 rounded-xl font-semibold text-base transition-all duration-150 ${[m
                           n === null ? 'invisible' : 'bg-zinc-800/60 text-zinc-300 hover:bg-zinc-700/60 active:bg-amber-500 active:text-black active:scale-95'[m
                         }`}>[m
                           {n}[m
                         </button>[m
                       ))}[m
                     </div>[m
[32m+[m[32m                    {loginError && ([m
[32m+[m[32m                      <div role="alert" className="mt-4 rounded-xl border border-red-500/30 bg-red-500/10 px-4 py-3 text-center text-sm font-medium text-red-200">[m
[32m+[m[32m                        {loginError}[m
[32m+[m[32m                      </div>[m
[32m+[m[32m                    )}[m
                     {loginByPin.isPending && ([m
                       <div className="flex items-center justify-center mt-4">[m
                         <Loader2 size={16} className="text-amber-500 animate-spin" />[m
[36m@@ -508,7 +533,7 @@[m [mexport default function CTapHub() {[m
                 </div>[m
               ) : ([m
                 <div className="screen-slide-in">[m
[31m-                  <button onClick={() => { setSelectedDept(null); setPin(""); }}[m
[32m+[m[32m                  <button onClick={() => { setSelectedDept(null); setPin(""); setLoginError(null); }}[m
                     className="text-amber-500 type-caption mb-6 flex items-center gap-1 hover:text-amber-400 transition-colors">[m
                     <ChevronLeft size={16} /> All departments[m
                   </button>[m
[36m@@ -533,7 +558,12 @@[m [mexport default function CTapHub() {[m
                           )}[m
                         </button>[m
                       ))}[m
[31m-                      {deptStaff.filter(s => s.status === "active").length === 0 && ([m
[32m+[m[32m                      {staffByDept.isError && ([m
[32m+[m[32m                        <div role="alert" className="rounded-xl border border-red-500/30 bg-red-500/10 px-4 py-3 text-center text-sm font-medium text-red-200">[m
[32m+[m[32m                          Could not load this department. Use Quick PIN login or try again.[m
[32m+[m[32m                        </div>[m
[32m+[m[32m                      )}[m
[32m+[m[32m                      {!staffByDept.isError && deptStaff.filter(s => s.status === "active").length === 0 && ([m
                         <p className="text-zinc-500 type-body text-center py-8">No active staff in this department</p>[m
                       )}[m
                     </div>[m
[36m@@ -557,20 +587,18 @@[m [mexport default function CTapHub() {[m
                     </div>[m
                     <div className="grid grid-cols-3 gap-2">[m
                       {[1,2,3,4,5,6,7,8,9,null,0,"\u232b"].map((n, i) => ([m
[31m-                        <button key={i} onClick={() => {[m
[31m-                          if (n === "\u232b") setPin(p => p.slice(0, -1));[m
[31m-                          else if (n !== null && pin.length < 4) {[m
[31m-                            const newPin = pin + n;[m
[31m-                            setPin(newPin);[m
[31m-                            if (newPin.length === 4) handlePinLogin(newPin);[m
[31m-                          }[m
[31m-                        }} className={`h-12 rounded-xl font-semibold text-base transition-all duration-150 ${[m
[32m+[m[32m                        <button key={i} onClick={() => handlePinPadPress(n)} className={`h-12 rounded-xl font-semibold text-base transition-all duration-150 ${[m
                           n === null ? 'invisible' : 'bg-zinc-800/60 text-zinc-300 hover:bg-zinc-700/60 active:bg-amber-500 active:text-black active:scale-95'[m
                         }`}>[m
                           {n}[m
                         </button>[m
                       ))}[m
                     </div>[m
[32m+[m[32m                    {loginError && ([m
[32m+[m[32m                      <div role="alert" className="mt-4 rounded-xl border border-red-500/30 bg-red-500/10 px-4 py-3 text-center text-sm font-medium text-red-200">[m
[32m+[m[32m                        {loginError}[m
[32m+[m[32m                      </div>[m
[32m+[m[32m                    )}[m
                     {loginByPin.isPending && ([m
                       <div className="flex items-center justify-center mt-4">[m
                         <Loader2 size={16} className="text-amber-500 animate-spin" />[m
[36m@@ -615,12 +643,21 @@[m [mexport default function CTapHub() {[m
                 </button>[m
               </div>[m
 [m
[31m-              {/* Facebook Login Button */}[m
[31m-              <button onClick={handleFacebookLogin} disabled={facebookLogin.isPending}[m
[31m-                className="w-full py-3.5 rounded-xl bg-[#1877F2] text-white font-semibold type-body hover:bg-[#166FE5] active:scale-[0.98] transition-all flex items-center justify-center gap-2 disabled:opacity-50">[m
[31m-                <svg className="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/></svg>[m
[31m-                {facebookLogin.isPending ? "Connecting..." : "Continue with Facebook"}[m
[31m-              </button>[m
[32m+[m[32m              {/* TODO(Facebook Login): Hidden in production until SDK loader + FB.init are implemented. */}[m
[32m+[m[32m              {ENABLE_FACEBOOK_LOGIN && ([m
[32m+[m[32m                <button onClick={handleFacebookLogin} disabled={facebookLogin.isPending}[m
[32m+[m[32m                  className="w-full py-3.5 rounded-xl bg-[#1877F2] text-white font-semibold type-body hover:bg-[#166FE5] active:scale-[0.98] transition-all flex items-center justify-center gap-2 disabled:opacity-50">[m
[32m+[m[32m                  <svg className="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/></svg>[m
[32m+[m[32m                  {facebookLogin.isPending ? "Connecting..." : "Continue with Facebook"}[m
[32m+[m[32m                </button>[m
[32m+[m[32m              )}[m
[32m+[m
[32m+[m[32m              {/* TODO(WebAuthn): Biometric/Face ID login remains hidden until WebAuthn is implemented. */}[m
[32m+[m[32m              {ENABLE_BIOMETRIC_LOGIN && ([m
[32m+[m[32m                <button type="button" className="hidden" aria-hidden="true" tabIndex={-1}>[m
[32m+[m[32m                  Continue with Face ID[m
[32m+[m[32m                </button>[m
[32m+[m[32m              )}[m
 [m
               <p className="text-center type-caption text-zinc-600">[m
                 Don't have an account?{" "}[m
[1mdiff --git a/server/_core/context.ts b/server/_core/context.ts[m
[1mindex 5eaa9df..db7f118 100644[m
[1m--- a/server/_core/context.ts[m
[1m+++ b/server/_core/context.ts[m
[36m@@ -5,7 +5,22 @@[m [mimport { ENV } from "./env";[m
 import * as jose from "jose";[m
 [m
 const STAFF_COOKIE_NAME = "staff_session";[m
[31m-const STAFF_JWT_SECRET = new TextEncoder().encode(ENV.cookieSecret || "fallback-staff-secret");[m
[32m+[m
[32m+[m[32mfunction getStaffJwtSecret(): Uint8Array {[m
[32m+[m[32m  const secret = ENV.cookieSecret.trim();[m
[32m+[m
[32m+[m[32m  if (!secret) {[m
[32m+[m[32m    if (ENV.isProduction) {[m
[32m+[m[32m      throw new Error("JWT_SECRET must be set in production for staff session signing.");[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    return new TextEncoder().encode("fallback-staff-secret");[m
[32m+[m[32m  }[m
[32m+[m
[32m+[m[32m  return new TextEncoder().encode(secret);[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32mconst STAFF_JWT_SECRET = getStaffJwtSecret();[m
 [m
 /** Sign a staff session JWT after PIN login */[m
 export async function signStaffSession(staffId: number): Promise<string> {[m
